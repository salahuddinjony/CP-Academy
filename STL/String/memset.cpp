#include<bits/stdc++.h>
using namespace std;
#define optimize() ios_base:: sync_with_stdio(0);cin.tie(0);cout.tie(0);
int main(){
    optimize();
    /*
    memset is useful for initializing arrays or structures 
    to a specific value.
    */
    

    char arr[10];
    memset(arr,'A', sizeof(arr)); // sets all elements of the array to ‘A’.
    for(int i=0;i<sizeof(arr);i++) cout<<arr[i]<<" ";



    return 0;
}



/*
 Author : SALAH 
"HARD WORK CAN CHANGE LUCK" 
*/